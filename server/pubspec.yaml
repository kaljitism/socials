name: server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  build_runner: ^2.4.12
  dart_frog: ^1.1.0
  postgres: ^2.5.2
  shared:
    path: ../shared
  stormberry:

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^6.0.0
